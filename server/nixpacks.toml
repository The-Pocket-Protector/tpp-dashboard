[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["cd .. && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd .. && pnpm --filter @paperclipai/server build", "cd .. && pnpm --filter @paperclipai/db build"]

[start]
cmd = "node dist/index.js"
